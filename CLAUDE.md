# WAZA KIMURA — 開発ルール（Claude用）

BJJ動画管理アプリ。**ユーザーの実データ（動画・カスタムビュー・ノート・タグ）を保持する本番運用中のツール**。
データはFirestore（クラウド・複数デバイス同期）と localStorage（端末ローカルキャッシュ）の二層で保持される。

## 🛑 最優先ルール: データ層の変更は高リスクとして扱う

過去に「ログアウト時にカスタムビューを非表示」という些細な見た目変更が、
`_views`空化→`saveUserSettings`→Firestore上書き→**全デバイスでカスタムビュー消失**を引き起こした（v52.541）。
教訓: *仕組みが動いた ≠ 結果が正しい*。局所のテストではなく**システム全体の帰結**を見る。

### 高リスク対象（これらに触れる変更は下記プロトコル必須）
- `js/firebase.js`（特に `saveUserData` / `saveUserSettings` / `loadUserSettings` / `onAuthStateChanged`）
- localStorage キーへの書き込み・**削除**（`wk_cv_views`, `wk_tagGroups`, `wk_*`, `waza_*` 等）
- `_views` / `window._cvViews` / `tagGroups` など**クラウド同期される状態**の書き換え
- あらゆる `removeItem` / `delete` / 配列の空化 / 既存データの上書き
- 認証状態（ログイン/ログアウト）に連動する処理

### コミット前プロトコル（必須）
1. **全データ経路を書き出す**: その変更がどの状態を変え、それが localStorage / Firestore / 他デバイス にどう波及するか。
2. **最悪ケースを問う**: 「これは他のデバイス/セッションに存在するデータを、消す・空で上書きする可能性があるか？」
   - YES の可能性が少しでもあれば、**空や欠損で上書きしない安全方向の実装**にする（非空→空の上書きは原則禁止）。
3. **メリットとブラスト半径を天秤にかける**。見た目改善程度のためにデータ破壊リスクを取らない。
4. 検証は「仕組みが動く」ではなく「**データが安全な結果になる**」を確認する。

## 🛑 ルール2: マージ運用 — オーナー承認済みで毎回 main へマージしてよい

オーナー（okujournal@gmail.com）から「今後はデータ層を含め確認不要で毎回本番マージしてよい」と恒久承認を得ている（2026-06-28）。
したがって通常の機能/UI変更も**高リスク対象に触れる変更も、ユーザー確認を取らずに main へマージ・デプロイしてよい**。

ただし承認されたのは「都度の確認を省くこと」であって「安全確認の省略」ではない。以下は引き続き必須:
1. **ルール1のコミット前プロトコル（全データ経路の書き出し・最悪ケース検証・非空→空の上書き禁止）は毎回必ず実施する**。安全方向の実装を徹底する。
2. データ層に触れる変更をマージしたら、**何を変えたか・どのデータに波及するかを返信で明示**する（事後報告）。
3. 破壊的変更が避けられない場合のみ、マージ前に一声かける（恒久承認の例外）。

## 🌐 多言語（i18n）ルール — 英語ユーザーも本番利用中
仕組み: `js/i18n.js` が表示層を自動翻訳（EN時のみ・MutationObserver）。**データは常に日本語のまま保存**し、表示だけ訳す。
ユーザーデータ（動画タイトル・メモ・ノート名・プレイリスト名等）は**絶対に翻訳しない**（contenteditable/入力値はエンジンが自動でスキップ）。

### 新しいUI文言を追加するとき（必須・同じコミットで）
1. **固定文** → `STATIC_AUTO` に `'日本語':'English'` を追加。
2. **数値入りテンプレ文**（`${n}本を追加` 等）→ `TEMPLATE_AUTO` に追加。キーは**数字列をすべて `#` に置換**したもの（例: `'✅ #件を追加しました':'✅ Added #'`。`MP4`の4や`1つ`の1も `#` になる点に注意）。
3. **文字列補間文**（名前が入る等）→ `AUTO_PATTERNS` に正規表現で追加。関数は `null` を返すと「このパターンでは訳さない」。
4. タグ分類（POSITIONS/カテゴリ/TB/TECHNIQUE_BUILTIN）は実行時に自動登録されるので何もしなくてよい。

### 確認方法
- `node tools/i18n-check.mjs` … コード全体から日本語を抽出しエンジンに通して未訳を列挙（ブラウザ不要）。
  既知の許容残: console.log・AIルール文・管理画面・連結断片（実行時はパターンが拾う）。
- 実行時の取りこぼしは英語モードで一通り操作後、コンソールで `wkI18nMissing()`。

## 不具合対応の原則
- **まず“見える化”してから直す**。原因を推測で順番に潰さない。最初の一手は観測（実エラー/状態を画面やログに出す）。
- 各メッセージを独立タスクとして処理せず、アプリ全体・データモデル・リスク面を握り続ける。

### 最初の一手は「壊れている成果物そのものを見る」
ログを足すことは見える化ではない。壊れたファイル・壊れた表示そのものを見る。
2026-09-17、YouTube字幕が途中で切れる件で、原因を推測で4回名指しして4回とも外した。
ユーザーが画面録画を送ってきて、字幕に `55 05:41:292 --> 05:55:762` が
**そのまま表示されている**のが見えた瞬間に原因が確定した（時刻の書式が崩れ、
その行が本文として扱われていた）。最初にそれを求めていれば1手で終わっていた。
- ユーザーに「壊れているところの画面を1枚見せてください」と**最初に**言う。無料で最速。
- 金額やトークン数から逆算した推定を重ねない。**推定の上に推定を積むと、間違っていても筋が通って見える。**

### 「途中から」の報告は、境界だけを見る
途中まで動いて止まるものは、境界の1行にすべてが出る。
合計・割合・カバー率を計算しない。**壊れる瞬間の1行**を見る。

### 故障モードは4つ全部を口に出してから潰す
**作られていない / 保存されていない / 読めていない / 描けていない。**
上記の件では「作られていない」「保存で捨てている」の2つだけを往復し、
実際の原因である「読めていない」を最後まで候補に入れなかった。

### 巻き戻して消えないなら、原因はそのコードではない
コードを分岐前とバイト一致まで戻しても症状が続いたのに、まだコードを探し続けた。
巻き戻しは最強の切り分け。結果が出たら仮説を捨てる。

### 無い情報は復元できない。直せないものは「直せない」と書く
2026-09-19、YouTube字幕の時刻がズレる件で、補正を4通り作って4通りとも外した
（一定倍率・区間アンカー・動画の長さに合わせる・つまみ）。
原因は1日目に取った実測データにすでに出ていた。キューの96%が隙間ゼロで前に直結、
開始のミリ秒の95%が000、字幕が無音として数えた時間は108秒なのに実際に足りないのは423秒。
**315秒ぶんの無音が、どこで起きたか分からないまま散っている**＝時刻に情報が入っていない。
無音は一定間隔で来ないのだから、ずらしても伸ばしても直らない。最初から直せなかった。
- 手がかりが本当にあるか、**データで先に確かめる**。無ければ作らない。
- 直せないものは、直せるふりのUIを作らずに「直せない」と画面に出す。
  黙って出した時刻は信じられてしまう。信じられる時刻でないなら、そう書く。
- 代わりに**成り立つ道**を示す（この件ならDriveに置く＝音声から実測する経路）。

### YouTube動画の字幕は、YouTube自身の字幕から作る（2026-09-19 実測で確立）
Geminiに動画を見せて作った時刻は音から測っていないので、後から直せない。
YouTube自身が持つ字幕は時刻が正確なので、それを土台に本文だけ訳す。
実測の差（1:07:58 の動画・同じ1本）:
  Gemini:   $0.339〜$0.381 / 304〜404秒 / 時刻がズレる
  YouTube:  $0.053 / 46秒 / 最後のキュー 1:07:59（＝尺とぴったり）
取得は業者のAPI（SUPADATA_API_KEY）経由。**Workerから直接は取れない**ことを実測で確認済み:
  it/web・mweb:LOGIN_REQUIRED / it/tv-embed:ERROR / it/ios・android:HTTP 400
  watch:HTTP 429 / embed:同意ページ
データセンターのIPを拒否されている形で、クライアントの名乗り方を変えても直らない。
未設定なら従来どおりGeminiに落ちるだけなので、壊れはしない。

### 「確定」と書かない
確度を口にした瞬間に探すのをやめる。根拠にした前提のうち**未検証のものを必ず併記**する。

### 自分で調べる前に、世界が既に答えを持っていないか探す
上記の件は全部、公式ドキュメントとGoogleのIssueに書いてあった
（YouTube URLを渡すと時刻がずれる／Geminiが訓練された時刻書式は `MM:SS` で
SRTの `HH:MM:SS,mmm` ではない／構造化出力(responseSchema)で形を強制するのが定石）。
目の前のコードだけを読んで推測する前に検索する。

### 同じものを指す値が2つあるなら、読む場所を1つにする
2026-09-19、「AI要約・字幕生成・自動チャプターのボタンが、出る動画と出ない動画がある」。
原因は保存されている中身の差だった。YouTube検索やプレイリスト取り込みで追加した動画は
`id` と `ytId` の両方を持つが、**URLを貼って追加した動画は `id` だけ**で `ytId` が無い
（`index.html` の追加経路が入れていなかった）。
ノートや整理の機能は昔から `v.ytId || v.id` と代用していたのに、**AI系の3つだけが
`v.ytId` を直接見ていた**ので、そこだけ機能が消えていた。動画IDは `id` にあり、
情報は揃っていた。見る場所が1つずれていただけ。
- 同じものを指す値が複数あるなら、**取り出しは1つの関数に集約**する（`_vYtId()`）。
  直接フィールドを読む場所を増やさない。検査で見張る（`yt-id-check`）。
- 入り口（保存するとき）でも揃える。読む側の代用は、既にあるデータのための救済。
- **一部の動画でだけ機能が消える**のは、たいていこの形。「YouTubeなのに違う」と言われたら
  まず保存されている中身の差を疑う。

## 既存の機能に新しい入口を足すときの原則
**新しい入口を、既存の別用途の引数・経路に相乗りさせない。**
2026-09-17、「チャプター作成時に字幕生成も挟む」という、既にある2つを順に呼ぶだけの
要望に対し、`vpGenSubtitle(id, preset)` の `preset`（一括処理の契約）へ相乗りさせた。
`preset` は「確認ダイアログを出さない・既存があれば飛ばす・既存の別言語から翻訳で
済ませる」を含むため、言語を聞かないだけのつもりが**ボタンを押した時と別の機能**になり、
そこから連鎖して字幕そのものを壊し、全部取り消すまで半日かかった。
- 入口は薄く保つ。**既存の関数は引数なしで、ボタンを押したのと同じに呼ぶ。**
- 既存の挙動が変わらないことを、足す前に確かめる。変わるなら、それは別の機能である。
- 依存は一方向に保つ（チャプターは字幕を呼ぶ／字幕はチャプターを知らない）。

## 検査（`node tools/<name>.mjs`）
壊した経路には検査を残す。ブラウザ不要・AI呼び出しなし・無料。
- `check-defs` … 宣言が消えていないか
- `smoke` / `sub-off-check` / `vp-dropdown-smoke` / `note-tpl-smoke` / `video-audit-smoke` … 画面の基本動作
- `videos-overwrite-smoke` / `backup-check` … データを壊さないこと
- `i18n-check` … 未訳の洗い出し
- `srt-tc-check` … 字幕の時刻書式が崩れても読めること／別の崩れ方は黙って保存しないこと
- `chap-sub-check` … 自動チャプターが字幕生成を「押したのと同じ」に呼んでいること
- `sub-show-check` … 作った字幕が、その場で選ばれた状態で表示されること
- `sub-timing-check` … 直せないものを直せるふりをしていないこと／時刻の出所を画面に出していること
- `yt-id-check` … YouTube動画IDの取り出しが1か所に集約されていること（AI系の機能が一部の動画で消えない）
- `gdu-smoke` … 端末アップロード

## デプロイ / バージョン
- 本番は **Cloudflare Worker**（`waza-kimura.okujournal.workers.dev`、`_worker.js` + 静的アセット）。`/api/*` は `_worker.js` が処理。
- 変更ごとに `index.html` の `<title>WAZA KIMURA vXX.XXX</title>` を上げる（デプロイ確認用）。
- 作業ブランチ: `claude/epic-ride-er1IM`。main push で本番デプロイ。
- PWAキャッシュがあるため、ユーザーには反映確認時にハードリロードを案内する。
